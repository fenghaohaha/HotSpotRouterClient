<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.haofeng.preresearch.satellitemodemclient.MainActivity" >

    <EditText
        android:id="@+id/et_ip"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="ip" />

    <EditText
        android:id="@+id/et_port"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="请输入4-5位数字"
        android:inputType="number"
        android:maxLength="5"
        android:minLines="4" />

    <Button
        android:id="@+id/bt_connect_server"
        style="@style/btn_style"
        android:text="连接服务器" />

    <Button
        android:id="@+id/bt_apply_authen"
        style="@style/btn_style"
        android:text="请求认证" />

    <Button
        android:id="@+id/bt_apply_call"
        style="@style/btn_style"
        android:text="请求通话" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="5dp"
        android:background="@drawable/btn_background_normal"
        android:padding="3dp" >

        <TextView
            android:id="@+id/tv_state_msg"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="5dp"
            android:scrollbarStyle="insideOverlay"
            android:scrollbars="vertical"
            android:text="日志信息：\n" />
    </ScrollView>

</LinearLayout>